/*global test expect:true*/
import React from 'react'; // eslint-disable-line
import { shallow } from 'enzyme';
import page500 from '../../pages/500';

test('should ', () => {
  const pageHome = shallow(page500); // eslint-disable-line
  expect(1).toBe(1);
});