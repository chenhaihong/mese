#!/usr/bin/env node
/*eslint no-console: 0 */

const program = require('commander');
const { version } = require('../package.json');

program
  .version(version, '-v, --version')
  .command('init', '创建模板项目').alias('i')
  .command('build', '生产模式下构建文件').alias('b')
  .command('watch', '监听模式下构建文件').alias('w')
  .command('test', '测试').alias('t')
  .command('serve', '启动服务').alias('s')
  .on('--help', function () {
    console.log('');
    console.log('Examples:');
    console.log('  $ mese init');
    console.log('  $ mese build');
    console.log('  $ mese test');
    console.log('  $ mese build && mese serve --port 3000');
  })
  .parse(process.argv);

if (!process.argv.slice(2).length) {
  program.help();
}