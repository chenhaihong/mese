const { join, resolve } = require("path");
const fse = require("fs-extra");

const root = resolve(__dirname, "..", "..");

// fse.removeSync(join(root, "example/dist"));
// fse.removeSync(join(root, "example/node_modules"));
