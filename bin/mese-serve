#!/usr/bin/env node

const program = require('commander');
const mese = require('..');

program
  .option('-p, --port [port]', '设置端口号', 3000)
  .option('-o, --open', '自动打开首页', false)
  .parse(process.argv);

const port = program.port;
const open = program.open;
mese.serve(port);