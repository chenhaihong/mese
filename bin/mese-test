#!/usr/bin/env node

const path = require('path');
const program = require('commander');
const test = require('../libJest');

program
  .option('-d, --directory [directory]', '待测试的目录', './')
  .parse(process.argv);

const dir = path.resolve(process.cwd(), program.directory);
test({ cwd: dir });